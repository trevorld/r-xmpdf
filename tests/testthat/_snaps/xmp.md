# get_xmp() / set_xmp()

    Code
      print(xmp())
    Output
      no XMP metadata found

---

    Code
      print(x)
    Output
      x:XMPToolkit : Image::ExifTool 12.40
      dc:Title : An XMP title

